# LLM-Model
AI-SQL RAG Chatbot: Built an AI-powered chatbot that converts natural language questions into SQL queries using LLM and RAG. The system retrieves database schema/context, generates accurate SQL queries, executes them on the database, and returns results in simple English.
